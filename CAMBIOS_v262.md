# APPI v262 · Histórico modo noche + píldoras

- Botones del Histórico (hero, mini, secondary, menú, picker, año) con paleta de modo noche.
- Las píldoras de valores de `lineChart` se clampean al viewBox para no recortarse en los extremos.
- `CACHE_NAME` = `appi-v262-historico-dark-pills`.
